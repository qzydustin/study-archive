/* mixed assignments to local int and char variables */

extern void println(int x);

void main(void) {
    int u, v;
    char x, y;

    x = 17;
    u = x;
    y = u;
    v = y;
    println(v);

    u = 255;
    x = u;
    v = x;
    y = v;
    println(y);
}
